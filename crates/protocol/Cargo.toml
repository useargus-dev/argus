[package]
name = "protocol"
version.workspace = true
description = "Argus IPC protocol types (v3 env fetch + v4 sandbox)"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
